---
# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json

modules:
  - type: dnf

    install:
      packages:
        - fprintd-clients
        - fprintd-clients-pam
        - open-fprintd
        - python3-validity
        - tlp
        - tlp-rdw

    remove:
      auto-remove: true
      packages:
        - tuned
        - tuned-ppd
