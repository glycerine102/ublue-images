---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json

modules:
- type: dnf

  install:
    packages:
    - tlp
    - tlp-rdw

  group-install:
    with-optional: true
    packages:
    - cosmic-desktop
    - cosmic-desktop-apps
