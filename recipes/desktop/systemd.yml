---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json

modules:
  - type: systemd
    system:
      enabled:
        - input-remapper.service
        - lactd.service
        - sanoid.timer
        - sshd.service
        - zfs-load-encrypted.service
        - zfs-scrub-monthly@storage.timer
