---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json

modules:
- type: dnf

  install:
    packages:
    - https://github.com/ilya-zlobintsev/LACT/releases/download/v0.8.4/lact-0.8.4-0.x86_64.fedora-43.rpm
    - input-remapper
    - mangohud

  group-install:
    with-optional: true
    packages:
    - virtualization
